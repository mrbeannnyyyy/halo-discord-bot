import Link from "next/link";import {Logo} from "@/components/ui";
export default async function Login({searchParams}:{searchParams:Promise<{next?:string}>}){const next=(await searchParams).next;const destination=next?.startsWith('/')&&!next.startsWith('//')?next:'/account';return <main className="auth"><Logo/><p className="eyebrow" style={{marginTop:40}}>Welcome back</p><h1>Your place starts here.</h1><p className="muted">Connect your Discord account to start or continue your application.</p><a className="button" style={{width:'100%',marginTop:22}} href={`/auth/discord?next=${encodeURIComponent(destination)}`}>▣ &nbsp; Continue with Discord</a><p className="muted" style={{fontSize:11,marginTop:20}}>We only use your Discord identity to secure your application. <Link href="/">Return home</Link></p></main>}
